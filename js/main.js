/**
 * Main Entry Point
 *
 * Initializes the application when DOM is ready
 */

document.addEventListener('DOMContentLoaded', function () {
  // Global ajaxurl is set by wp_localize_script in PHP
  // AppController is loaded last and initializes automatically
});
